// components/ui/SectionDivider.js
// Thin HR with uppercase label

export default function SectionDivider({ label }) {
  return (
    <div className="section-divider">
      <span>{label}</span>
    </div>
  );
}
